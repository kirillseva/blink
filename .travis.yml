language: r
sudo: required
warnings_are_errors: true

services:
  - redis-server

r_github_packages:
  - jimhester/covr
  - hadley/devtools
  - bwlewis/rredis
  - hadley/plyr
  - hadley/dplyr
after_success:
  - Rscript -e 'covr::codecov()'
